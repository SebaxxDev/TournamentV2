# Frontend - Tournament Manager V2

Aplicacion web del sistema Tournament Manager V2.

## Desarrollo rapido

Desde la carpeta `frontend`:

```bash
npm install
npm run dev
```

Scripts:

- `npm run dev` - servidor de desarrollo (Vite)
- `npm run build` - build de produccion
- `npm run preview` - previsualizacion del build
- `npm run lint` - lint de codigo

## Documentacion principal

La guia completa del proyecto (backend, variables de entorno, scripts globales y estructura) esta en:

- `../README.md`
